#!bin/bash
set -e

#install typst
snap install typst

# Install Jupyter extensions
jupyter labextension install jupyterlab-plotly

# Initialize MyST environment
myst init

# Convert notebooks to MyST Markdown (if needed)
# jupytext --to myst *.ipynb

# Precompile assets
mkdir -p _build
